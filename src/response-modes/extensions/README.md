# 扩展响应模式（extensions/）

用户自定义响应模式，type: 'extension'，由 `registry.registerExtension()` 注册。

与 `core/`（内置）物理隔离：扩展模式不可覆盖内置模式 id。

开发指南：`docs/response-system/plugin-guide.md`（含 30 分钟快速上手）。

当前为空目录，扩展模式由使用者按需添加。
