# Backlog & 问题清单 — 2026-05-17

整理自与夙夜无偕的多轮澄清对话。

---

## A. 文件发送

1. **Agent 发文件一次操作成功** — Agent 发送文件时，对方很难一次操作成功，需要解决发送流程。
2. **收到文件不可见** — 收到的文件，不退出再进会话，看不到；需要修复实时刷新/展示逻辑。

---

## B. CLI 工具（优先级高）

3. **实现 Agent 管理 + 消息收发 CLI** — 这是当前最优先的工作，需先把这套工具做出来。
4. **从 `ctl` 移除 Agent 管理命令** — 配置好直接可用，不需要 `reload` 命令；移除前需先搞清楚为什么现在需要 `reload`。

---

## C. 群组机制

5. **群改为提及模式（@mention）** — 当前群里只要有一个 agent 持续说话就停不下来；需改成：
   - 提及模式：被 @ 才响应
   - 分发模式：消息按规则分发给指定 agent
   - Agent 模式：agent 自主决定是否响应

---

## D. 消息展示

6. **消息数量展示不准确** — Agent 收到的消息数量展示有误，需修正计数逻辑。
7. **会话内跳转到上次位置** — 会话里应支持跳到"上次看到的位置"（类似未读标记/已读位置）。

---

## E. Agent 管理统一

8. **两套 Agent 管理体系需统一** — 当前存在两套体系，需系统性梳理后合并。具体细节做的时候再展开。

---

## F. 身份层

9. **身份层最后加入** — Agent 管理统一之后，才开始加入身份层相关功能。

---

## G. AUN Context Kit 维护

10. **文档指向错误** — AUN Context Kit 中某文档当前指向的命令行工具文档路径有误，指向的是 KIT 目录下的文件，应改为指向 evolclaw 的正确文档。

---

## 优先顺序（初步）

1. 搞清楚 `reload` 为何存在 → 移除 `ctl` 中的 Agent 管理命令
2. 实现 Agent 管理 + 消息收发 CLI
3. 修复文件发送（Agent 发文件 + 收到文件可见性）
4. 群组提及模式
5. 消息数量 + 会话跳转
6. 两套 Agent 管理体系统一
7. 身份层
8. AUN Context Kit 文档修正
