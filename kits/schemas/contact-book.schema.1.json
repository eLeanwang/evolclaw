{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "contact-book.schema.1.json",
  "title": "ContactBook (agents/{aid}/contact.json)",
  "x-logical-name": "contact-book",
  "x-scope": "contact",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "$schema_version": { "type": "number", "default": 1, "x-merge": "scalar" },
    "contacts": {
      "type": "object",
      "x-merge": "dict",
      "description": "联系人字典，键为对端 AID",
      "propertyNames": {
        "pattern": "^([A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?\\.){2,}[A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "displayName": { "type": "string", "description": "显示名" },
          "aliases": {
            "type": "array",
            "description": "跨渠道别名，格式 <channel>:<原生ID>",
            "items": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]+:.+$"
            },
            "uniqueItems": true
          }
        }
      }
    }
  }
}
