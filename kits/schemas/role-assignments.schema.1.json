{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "role-assignments.schema.1.json",
  "title": "RoleAssignmentsConfig (agents/{aid}/role-assignments.json)",
  "description": "Per-agent source of truth for peer role assignments.",
  "x-logical-name": "role-assignments",
  "x-permission": "H",
  "x-scope": "role-assignments",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema_version", "assignments"],
  "properties": {
    "$schema_version": {
      "type": "number",
      "const": 1,
      "description": "Schema version",
      "x-merge": "scalar"
    },
    "assignments": {
      "type": "object",
      "description": "Map keyed by `${channelKey}::${peerId}`.",
      "x-merge": "dict",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["channelKey", "peerId", "role"],
        "properties": {
          "channelKey": { "type": "string" },
          "peerId": { "type": "string" },
          "role": { "type": "string" },
          "note": { "type": "string" },
          "createdAt": { "type": "number" },
          "updatedAt": { "type": "number" }
        }
      }
    }
  }
}
