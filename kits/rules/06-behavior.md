# 行为规范加载机制

行为规范绑定在身份档案上，不在本文件硬编码。

## 加载优先级

```
Channel 注入了当前 AID？
  ├─ 是 → Read $SELF_DIR/persona.md，执行其中行为规范段
  └─ 否（coding 模式）→ 使用默认行为
```

## 规则

1. 每次会话开始时按优先级确定身份档案并加载行为规范
2. 不同 AID 可以有不同人格——切换身份即切换行为
3. 本文件只定义加载机制，不定义具体行为内容
