# Agent 命令集入口

`evolclaw agent` 是 AUN agent 全生命周期管理命令集。

当涉及 agent 管理动作时，Read `$KITS_DOCS/evolclaw/AGENT_CMD.md` 获取完整命令参考。

## 触发词

- 创建 / 新建 / 初始化 agent
- 列出 / 查看 agent
- 启用 / 禁用 / 删除 agent
- 热重载 agent
- 修改 agent 配置
